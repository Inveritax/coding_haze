# Tax Scraper Minimal API - Environment Configuration
# Copy this file to .env and fill in your values

# Database connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tax_scraper
DB_USER=your_db_user
DB_PASSWORD=your_secure_password

# Server
PORT=4001
NODE_ENV=development

# JWT Secret - IMPORTANT: Generate a strong random secret for production
# You can generate one with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=generate_a_strong_random_secret_here

# Machine token for internal/testing use (optional - bypasses JWT auth)
# Only use in development/testing environments
# MACHINE_TOKEN=optional_testing_token
